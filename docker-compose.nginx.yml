version: '3.9'

services:
  inknexus-nginx:
    build:
      context: ./front
      dockerfile: Dockerfile
    container_name: inknexus-nginx
    ports:
      - "80:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
